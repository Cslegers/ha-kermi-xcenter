-r requirements_lint.txt
colorlog==6.10.1
homeassistant==2026.9.1
# Pulled in at runtime by the modbus integration this depends on;
# pinned here so a development instance starts without a fetch.
modbus-connection[tmodbus]==4.11.1
pymodbus==3.13.1
tmodbus==0.6.2
